/**
 * Bumped whenever the on-disk JSON shapes change in a non-backwards-compatible way.
 * Projects with a lower version trigger a migration before being opened.
 */
export const FORMAT_VERSION = 1;
